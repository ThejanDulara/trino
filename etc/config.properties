coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080
discovery.uri=http://localhost:8080

# ??? keep concurrency tiny to avoid thread explosions
task.max-worker-threads=2                 # reduce worker task threads
task.min-drivers=4                        # small target of running splits

# ??? cap memory so a single query can?t hog the heap
query.max-memory-per-node=2GB
query.max-total-memory-per-node=512MB
memory.heap-headroom-per-node=256MB
